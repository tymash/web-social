import Vue from 'vue'
import Vuetify from "vuetify";

Vue.use(Vuetify)

const opts = {}

export default new Vuetify({
    icons: {
        iconfont: 'md', // default - only for display purposes
    },
})